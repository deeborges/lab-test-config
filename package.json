{
  "scripts": {
    "start": "node server.js",
    "dev": "SET NODE_CONFIG_ENV=development&&SET NODE_ENV=development&& nodemon server.js",
    "qa": "SET NODE__CONFIG_ENV=qa && SET NODE_ENV=qa && node server.js",
    "prod": "set NODE_CONFIG_ENV=production && export NODE_ENV=production && node server.js"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "config": "^3.3.1",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "generate-gitignore": "^0.2.6",
    "mongodb": "^3.6.0",
    "mongoose": "^5.9.28"
  },
  "devDependencies": {
    "prettier": "2.0.5"
  }
}
